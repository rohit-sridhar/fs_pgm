## fs_pgm
### Introduction

fs_pgm is a python wrapper around a set of hidden markov model modules written in C++.

### Installation
Run `pip install -e .` to install.

#### Build scripts
pyproject.toml - contains 

